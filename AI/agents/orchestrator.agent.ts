export async function processImageToRecipe(formData: FormData, userId: string) {
  // Dummy implementation for orchestrator agent
  return { recipeId: 1, message: "Recipe generated successfully (mock)" };
}
